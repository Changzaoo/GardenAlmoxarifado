<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fundo preto -->
    <item android:drawable="@color/splashBackgroundDark"/>
    
    <!-- Logo centralizado -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher"/>
    </item>
    
    <!-- Indicador de carregamento na parte inferior -->
    <item android:bottom="80dp" android:gravity="bottom|center_horizontal">
        <shape android:shape="rectangle">
            <solid android:color="@color/splashPrimary"/>
            <size android:width="60dp" android:height="4dp"/>
        </shape>
    </item>
</layer-list>
